To install copy hlist.py to ~/.nautilus/python-extensions and restart nautilus.

IMPORTANT:
Settings' window doesn't work now, you have to automatically change settings in the configuration file.
Open with a text editor ~/.hash_tab_conf and set to 1 the hash-type you want.
